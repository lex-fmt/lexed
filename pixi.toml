# pixi workspace — seeded by `shipit install` (the managed blocks below
# need a valid manifest). Consumer-owned from here on: edit freely.
[workspace]
name = "lex-fmt_lexed"
channels = ["conda-forge"]
platforms = ["linux-64", "linux-aarch64", "osx-64", "osx-arm64"]

[tasks]
# >>> shipit-managed test task (do not edit; regenerate via `shipit install`) >>>
test = "./bin/shipit test"
# <<< shipit-managed test task <<<
# >>> shipit-managed tasks (do not edit; regenerate via `shipit install`) >>>
changelog = "./bin/shipit changelog"
lint = "./bin/shipit lint"
logs = "./bin/shipit logs"
provision-lexd = "./bin/shipit provision lexd"
# <<< shipit-managed tasks <<<

# Consumer-owned tasks (outside the shipit-managed blocks above).
# The wf-checks TEST LANE's run (ADP02-WS03, #168; modeled on lex's test-full):
# the wf-checks block's checkout provisions nothing beyond pixi, so the lane
# task SELF-PROVISIONS everything the unit suite needs before dispatching to
# the managed `test` task's implementation (`./bin/shipit test` -> `npm test`
# -> vitest):
#   - `npm ci` — the repo's node_modules (node/npm ride pixi's managed node
#     deps block below).
#   - `npm ci --prefix shared` — `@lex/shared` is a `file:./shared` dep whose
#     runtime deps (vscode-jsonrpc) live in its own lockfile.
# Deliberately NOT run: `npm run build --prefix shared` (shared/dist is
# COMMITTED and the suite is green on it; a tsc rebuild rewrites the
# prettier-formatted committed dist in place, dirtying the tree and failing
# the web lint gate), the comms submodule, and the fetch-deps artifacts
# (resources/lexd-lsp, grammar WASM) — those are the e2e/build surface, which
# stays on the companion e2e.yml workflow.
# Each `npm ci` is a clean, lockfile-exact reinstall that WIPES any existing
# node_modules — deliberately, so this run mirrors the CI test lane
# byte-for-byte (reproducible, not incremental; it does not reuse an already-
# installed tree). For fast local iteration run the `test` task
# (`./bin/shipit test`) directly against a tree you have already installed.
test-full = "npm ci && npm ci --prefix shared && ./bin/shipit test"

[feature.lint.dependencies]
# >>> shipit-managed lint deps (do not edit; regenerate via `shipit install`) >>>
# The fleet-pinned lint toolchain (arthur-debert/shipit docs/legacy-prd/adoption.md: the managed set owns
# the consumer environment). Canonical versions live HERE — a bump is one data
# edit, rolled fleet-wide on each consumer's next `shipit install` reconcile.
ruff = "0.15.*"
# shellcheck is PINNED DOWN to 0.10.* (not 0.11): shellcheck 0.11.0 has an SC2218
# reaching-definitions false positive — a call to an EARLIER-defined function is
# flagged "defined later" when a subshell (`$(...)`, a pipeline component, `<(...)`)
# sits near the call, reddening the hermetic gate on CORRECT code across 5+ fleet
# repos. 0.11.0 is the latest upstream (no fixed 0.11.x yet); restore 0.11+ when
# upstream fixes it.
shellcheck = "0.10.*"
# conda-forge name for the `shfmt` binary
go-shfmt = "3.*"
yamllint = "1.38.*"
prettier = "3.8.*"
markdownlint-cli = "0.49.*"
# GitHub Actions workflow gate — the actions Lang of `shipit lint` (TOL01-WS04)
actionlint = "1.7.*"
lefthook = "2.1.*"
# <<< shipit-managed lint deps <<<

[feature.lint.tasks]
# The wf-checks LINT LANE's run (ADP02-WS03, #168), byte-modeled on lex's /
# shipit's own: the lint toolchain (ruff/shfmt/prettier/markdownlint/
# actionlint) lives in THIS feature's env, so the lane task must resolve into
# the lint env — the managed bare `lint` task is a fixed plain string that
# runs in the default env (where none of those tools exist on a hosted
# runner) and cannot provision the lex leg's lexd. `provision lexd` inline
# lands lexd in this env's prefix (this repo tracks .lex files — README.lex,
# test fixtures — so the lex Lang of `shipit lint` runs), idempotent. The
# comms submodule is inited inline (the shipit#759 pattern, lex's test-full):
# two tracked fixtures (tests/fixtures/benchmark.lex, empty.lex) are SYMLINKS
# into comms/, and the wf-checks checkout has no submodule support — without
# it, `lexd check` fails on the dangling links (proven on this PR's first
# remote run). Defined ONLY in this feature, so it exists in EXACTLY ONE env
# (lint) and bare `pixi run lint-full` resolves unambiguously — the wf-checks
# matrix job's `pixi run --locked lint-full` and a laptop run are identical.
lint-full = { cmd = "git submodule update --init comms && ./bin/shipit provision lexd && ./bin/shipit lint" }

[environments]
# >>> shipit-managed environments (do not edit; regenerate via `shipit install`) >>>
lint = ["lint"]
# <<< shipit-managed environments <<<

[dependencies]
# >>> shipit-managed node deps (do not edit; regenerate via `shipit install`) >>>
# The fleet-pinned node runtime (#547 Layer 1) — delivered because this repo
# tracks a package.json. Unlike the rust/go blocks this is NOT a linter
# toolchain: it provisions the repo's OWN node/pnpm runtime (npm rides the
# nodejs package), which is why it lives in the DEFAULT env's [dependencies]
# rather than the lint feature. If this repo no longer ships node (the last
# package.json is gone), delete this block and its `pixi.toml#shipit-node-deps`
# entry in .shipit.toml's [managed] table — a reconcile only ADDS toolchain
# blocks, it never retires one.
nodejs = "26.*"
pnpm = "11.*"
# <<< shipit-managed node deps <<<
# Consumer-owned dependencies (outside the shipit-managed block above).
# uv backs the pinned bin/shipit launcher (ADR-0033: it provisions the repo's
# pinned shipit via uv, PATH-independent). Both wf-checks lanes run
# `pixi run --locked <task>` -> ./bin/shipit on a hosted runner where uv is
# not otherwise present, so the launcher's one prerequisite must ride the env
# too. (Known upstream gap — the block/managed set should own this: shipit#758,
# first flagged by lex#828/#829.)
uv = ">=0.5"
